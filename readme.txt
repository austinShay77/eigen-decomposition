Libraries needed:

    numpy
    math
    Pillow
    matplotlib
    openvc-python
    
To run the program:

    The listed libraries need to be installed, and a folder named yalefaces with the data needs to be present at the same level as
    the hw1.py file.

    To run the program: python3 ./hw1.py

    On completion, a png named part2 should be generated along with a video named part3